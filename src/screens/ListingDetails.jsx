import { View, Text } from 'react-native'
import React, {memo} from 'react'

const ListingDetails = memo(() => {
  return (
    <View>
      <Text>ListingDetails</Text>
    </View>
  )
})

export default ListingDetails